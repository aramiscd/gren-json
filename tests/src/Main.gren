module Main exposing ( main )

import FileSystem
import Init
import Node
import Test.Runner.Effectful exposing ( run )
import Tests exposing ( tests )


main : Node.SimpleProgram a
main =
    Node.defineSimpleProgram
    <| \ env -> Init.await FileSystem.initialize
    <| \ fsPermission -> run env
    <| tests fsPermission
